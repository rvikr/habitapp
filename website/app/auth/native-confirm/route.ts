// New native-only email link target. If App/Universal Link verification is not
// available (or an older app is installed), preserve the legacy website handoff
// to the custom scheme with a PWA fallback.
export { GET } from "../confirm/route";
