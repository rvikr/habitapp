// Dedicated App Link / Universal Link target for authentication emails created
// by the native app. PWA emails use /app/auth/callback and are intentionally not
// claimed by the native application.
export { default } from "./callback";
